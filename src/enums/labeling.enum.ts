export enum CanvasDataType {
  SAM = 'sam',
  BOX = 'box',
  BRUSH = 'brush',
  ENCODED = 'encoded',
  KEY_POINT = 'key_point',
}

export enum TaskActionEnum {
  CREATE = 'create',
  DELETE = 'delete',
}

export enum SamPointTypeEnum {
  NEGATIVE = 0,
  POSITIVE = 1,
}
